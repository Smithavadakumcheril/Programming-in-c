#include <stdio.h>
void main()
{
int n,i,x,count=0,a[100];
printf("enter the elements of the array");
scanf("%d",&n);
printf("enter the elements");
for(i=0;i<n; i++);
{ 
       scanf ("%d",&a[i]);
       }
       printf("enter the search element : ");
       scanf("%d",&x);
       for (i=0;i<n;i++);
       { 
           if(a[i]==x)
           {
           count++
           }
      {     
           if (count>0)
           
               
               } 
              else 
             
                printf("item is not found");
                }
                
                
                
